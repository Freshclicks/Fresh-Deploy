---
- hosts: all
  connection: local
  become: sudo

  roles:
    - common
    - fail2ban
    - ferm
    - ssmtp
    - php
    - memcached
    - mariadb
    - { role: nginx, nginx_pagespeed_enabled: yes, nginx_version: 1.12.2}
    - wp-cli
    - { role: sites, enviro: php, tags: [ 'sites' ] }
